# Локальный PostgreSQL: docker compose up -d
services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: hackdb
      POSTGRES_USER: hack
      POSTGRES_PASSWORD: hack
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
