# Empty uses Vite /api proxy. No secrets in VITE_* values.
VITE_API_BASE_URL=
# Development proxy target; restart Vite after changes.
BACKEND_PROXY_TARGET=http://localhost:8080
# Existing homepage API client setting, retained for compatibility.
VITE_API_URL=
